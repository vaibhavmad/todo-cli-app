[project]
name = "todo-app-cli-version"
version = "0.1.0"
description = """A simple command-line ToDo application built with Python,
supporting add, edit, complete, and view operations with persistent file storage."""
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
